{
  "name": "chrome-ext-react-next",
  "version": "1.0.0",
  "description": "A React + Next.js template for Create Chrome Ext.",
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export && node scripts/post-build.js"
  },
  "dependencies": {
    "react": "^18",
    "react-dom": "^18",
    "next": "^14"
  },
  "devDependencies": {
    "fs-extra": "^11.2.0"
  }
}